#!/bin/sh
#
# Display current Volume level
# Best used with my sxhkd config

dunstctl close-all && \

dunstify "`echo 'Volume' ; echo ' ' ; amixer sget Master | grep 'Right:' | awk -F'[][]' '{ print $2 }'`" -t 500
