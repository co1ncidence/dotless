#! /bin/sh
#
# Push a repo to master branch with a message you give
# Uses date if no message is given
#

[ -z "$1" ] && \
    msg="$(date +%H:%M:%S)" || \
    msg="$1"

git add .
git commit -m "${msg}"
git push origin master
