#!/bin/sh
#push a repo to master branch

[ -z "$1" ] && \
    msg="docs:update" || \ #use docs:update by default
    msg="$1" #user-defined commit message

git add .
git commit -m "${msg}"
git push origin master
