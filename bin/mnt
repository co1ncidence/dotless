#!/bin/sh
#mount/unmount /dev/sdb

alias sudo="sls -u root"

case "$1" in

	-m)
		mkdir -p "$HOME/mnt/" && \
		sudo mount "/dev/sdb" "$HOME/mnt" && \
		herbe "mounted USB" ;;

	-u)
		sudo umount "/dev/sdb" && \
		rm -rf "$HOME/mnt" && \
		herbe "unmounted USB" ;;

esac
