#!/bin/sh
#
# Simple script to vibe
# to a random song
#

exec mpv --geometry=200x200+1600+800 \
         --x11-name=no-title \
         --no-video \
         --shuffle \
         -- "$@" "$HOME/usr/mus/"
