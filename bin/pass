#!/bin/sh
#
# Dmenu script to copy my passwords to clipboard
#

dir="$HOME/usr/doc/savefiles/pass"

pass=$(ls -1 "$dir" | dmenu -fn 'Cascadia Code' -nb '#050505' -nf '#d9d9d9' -sb '#f0f0f0' -sf '#050505')

[ -f "$dir/$pass" ] && xclip -sel clip "$dir/$pass"
