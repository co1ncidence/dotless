#!/bin/sh
# dmenu script to copy my passwords to clipboard

dir="$HOME/usr/doc/sav/pass"

pass=$(ls -1 "$dir" | dmenu -fn "Recursive Mono Linear Static" -nb '#1c1b1d' -nf '#de9dac' -sb '#de9dac' -sf '#121213')

[ -f "$dir/$pass" ] && xclip -sel clip "$dir/$pass"
