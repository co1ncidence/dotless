#!/bin/sh
#dmenu script to copy weird gifs to clipboard

dir="$HOME/med/pic/mem/gif/"

gif="$(ls -1 "$dir" | dmenu -fn "Input Mono Condensed" -nb "#393939" -nf "#ffffff" -sb "#ffffff" -sf "#393939")"

[ -f "$dir/$gif" ] && xclip -sel clip -in "$dir/$gif"
