#!/bin/sh
#homedir roomba

[ "$(pgrep -x ch)" = "$$" ] || exit 1

while :; do
	set --
	for i in "$HOME/.pki" \
           "$HOME/.local" \
           "$HOME/.cache" \
           "$HOME/.config" \
           "$HOME/.xournalpp" \
           "$HOME/.lyrics" \
           "$HOME/.bash*" \
           "$HOME/.local"; do
		[ -e "$i" ] && set -- "$@" "$i"
	done
	[ "$1" ] && rm -rf -- "$@"
	sleep 60
done
