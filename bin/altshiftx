#!/bin/sh
#power menu

pwr="$(printf "suspend\nreboot\npoweroff\n'             '" \
				| dmenu -fn 'Iosevka Term Slab' -nb '#393939' \
																				-nf '#ffffff' \
																				-sb '#ffffff' \
																				-sf '#393939')"
[ "$pwr" ] && systemctl "$pwr"
