#!/bin/sh
# dmenu logout script

choice=$(echo "logout\nrestart\nshutdown\nsuspend\n'               '"| \
  dmenu -fn "Recursive Mono Casual Static" -nb '#1c1b1d' -nf '#fdf9f7' -sb '#fdf9f7' -sf '#121213')

case "$choice" in
  logout)     openbox --exit      ;;
  restart)    systemctl reboot    ;;
  shutdown)   systemctl poweroff  ;;
  suspend)    systemctl suspend   ;;
esac
