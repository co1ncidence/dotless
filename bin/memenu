#!/bin/sh
# dmemenu

dir="$HOME/usr/med/pic/mem"

mem=$(ls -1 "$dir" | dmenu -fn "iA Writer Mono V" -nb '#1c1b1d' -nf '#fdf9f7' -sb '#fdf9f7' -sf '#121213')

[ -f "$dir/$mem" ] && xclip -sel clip -t image/png "$dir/$mem"
