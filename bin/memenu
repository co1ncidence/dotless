#!/bin/sh

dir="$HOME/usr/pic/mem"

mem=$(ls -1 "$dir" | dmenu -fn 'Cascadia Code' -nb '#050505' -nf '#d9d9d9' -sb '#f0f0f0' -sf '#050505')

[ -f "$dir/$mem" ] && xclip -sel clip -t image/png "$dir/$mem"
