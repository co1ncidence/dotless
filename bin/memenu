#!/bin/sh
# dmemenu

dir="$HOME/med/pic/mem"

mem="$(ls -1 "$dir" | dmenu -fn "Recursive Mono Linear Static" -nb "#211d1c" -nf "#ede3e1" -sb "#ede3e1" -sf "#211d1c")"

[ -f "$dir/$mem" ] && xclip -sel clip -t image/png "$dir/$mem"
