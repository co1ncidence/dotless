#!/bin/sh
#dmemenu

dir="$HOME/med/pic/mem"

mem="$(ls -1 "$dir" | dmenu -fn "Iosevka Term Slab" \
                                      -nb "#363636" \
                                      -nf "#515151" \
                                      -sb "#363636" \
                                      -sf "#ffffff")"

[ -f "$dir/$mem" ] && xclip -sel clip -t image/png "$dir/$mem"
