#!/bin/sh
#power menu

pwr="$(printf "suspend\nreboot\npoweroff" \
  | dmenu -fn "Iosevka Term Slab" \
                    -nb "#3e393b" \
                    -nf "#7b7274" \
                    -sb "#3e393b" \
                    -sf "#e4e1e2")"
[ "$pwr" ] && systemctl "$pwr"
